import React from "react";
import { createRoot } from "react-dom/client";
import TravelExpenseFormDE from "./TravelExpenseFormDE.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TravelExpenseFormDE />
  </React.StrictMode>
);
